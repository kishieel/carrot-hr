const initialState = {
	employeeNextId: 1,
	employeeList: {}
}

const employeesReducer = ( state = {}, action ) => {
	switch ( action.type ) {
		default:
			return state
	}
}

export default employeesReducer
